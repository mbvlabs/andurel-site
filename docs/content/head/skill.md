# skill

`andurel skill` shows or installs the embedded Andurel agent skill.

```bash
andurel skill show
andurel skill install
```

Install once per harness so agents discover commands, generators, and project layout without inventing paths. See [Agentic Development](/docs/head/agentic-development).
